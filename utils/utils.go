package utils

import (
	"fmt"
)

func CharCount(rdr []byte) {
	fmt.Println(len(rdr))
	// for i := 0; i < len(rdr); i++ {
	// 	totalCh:=len(rdr)
	// 	totalWord:=""

	//	}

}
func wordcount(rdr []byte) {
	count := 0
	for i := 0; i < len(rdr); i++ {
		count += 1

	}
	return

}
